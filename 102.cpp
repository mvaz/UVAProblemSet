#include <iostream>
#include <sstream>
#include <cstdlib>
#include <vector>

using namespace std;


int main() {

	// istream is;
	int b1, b2, b3, g1, g2, g3, c1, c2, c3;
	
	// get the pairs of integers
	while( scanf("%d %d %d %d %d %d %d %d %d\n", &b1, &g1, &c1,
												 &b2, &g2, &c2,
												 &b3, &g3, &c3) == 9) {
		
	}

	exit(0);
}
